<form method="post" action="{{ path('presse_v_delete', {'id': presse_v.id}) }}" onsubmit="return confirm('Are you sure you want to delete this item?');">
    <input type="hidden" name="_method" value="DELETE">
    <input type="hidden" name="_token" value="{{ csrf_token('delete' ~ presse_v.id) }}">
    <button class="btn">Delete</button>
</form>
