{% extends 'base.html.twig' %}

{% block title %}Fiches{% endblock %}

{% block body %}
    <h1>Fiches</h1>

    <table class="table">
        <tbody>
            <tr>
                <th>Id</th>
                <td>{{ fich.id }}</td>
            </tr>
            <tr>
                <th>Titre</th>
                <td>{{ fich.titre }}</td>
            </tr>
            <tr>
                <th>Sous_titre</th>
                <td>{{ fich.sousTitre }}</td>
            </tr>
            <tr>
                <th>Image</th>
                <td>{{ fich.image }}</td>
            </tr>
            <tr>
                <th>Pdf</th>
                <td>{{ fich.pdf }}</td>
            </tr>
            <tr>
                <th>CreatedAt</th>
                <td>{{ fich.createdAt ? fich.createdAt|date('Y-m-d H:i:s') : '' }}</td>
            </tr>
        </tbody>
    </table>

    <a href="{{ path('fiches_index') }}">back to list</a>

    <a href="{{ path('fiches_edit', {'id': fich.id}) }}">edit</a>

    {{ include('fiches/_delete_form.html.twig') }}
{% endblock %}
