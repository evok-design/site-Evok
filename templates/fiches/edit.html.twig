{% extends 'base.html.twig' %}

{% block title %}Edit Fiches{% endblock %}

{% block body %}
    <h1>Edit Fiches</h1>

    {{ include('fiches/_form.html.twig', {'button_label': 'Update'}) }}

    <a href="{{ path('fiches_index') }}">back to list</a>

    {{ include('fiches/_delete_form.html.twig') }}
{% endblock %}
