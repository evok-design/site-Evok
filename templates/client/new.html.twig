{% extends 'baseAdmin.html.twig' %}

{% block title %}New Client{% endblock %}

{% block body %}
    {% include 'admin/menu.html.twig' %}

    <div class="container">
        <div class="row mt-3 mb-5">
            <div class="col">
                <a href="{{ path('client_index_admin') }}" class="btn btn-secondary"> <i class="fa fa-chevron-left"></i> Retour à la liste</a>
            </div>
        </div>
        <div class="row">
            <div class="col">
                {{ include('client/_form.html.twig') }}
            </div>
        </div>
    </div>

{% endblock %}
