{{ form_start(form) }}
    {{ form_row(form.titre, {attr : {"class" : "form-control"} } ) }}
    {{ form_widget(form) }}
    <button class="btn btn-success mt-2">{{ button_label|default('Ajouter') }}</button>
{{ form_end(form) }}


