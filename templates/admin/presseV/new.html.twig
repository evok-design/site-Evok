{% extends 'baseAdmin.html.twig' %}

{% block title %}Ajouter un article de presse video{% endblock %}

{% block body %}
    {% include 'admin/menu.html.twig' %}

    <div class="container">
        <div class="row mb-2">
            <div class="col text-left">
                <a href="{{ path('presse_v_index_admin') }}" class="btn btn-secondary"><i class="fal fa-angle-left"></i> Retour à la liste</a>
            </div>
        </div>

        <div class="row">
            <div class="col">
                {{ include('admin/presseV/_form.html.twig', {'button_label': 'Ajouter'}) }}
            </div>
        </div>
    </div>



{% endblock %}
