{% extends 'baseAdmin.html.twig' %}

{% block title %}Edit Slide{% endblock %}

{% block body %}
    {% include 'admin/menu.html.twig' %}

    <div class="container">
        <div class="row mb-2">
            <div class="col text-left">
                <h1>Modifier la photo</h1>
                <a href="{{ path('slide_index') }}" class="btn btn-secondary"><i class="fal fa-angle-left"></i> Retour à la liste</a>
            </div>
        </div>
        <div class="row">
            <div class="col">
                {{ include('admin/slide/_form.html.twig', {'boutton_label': 'ajouter'}) }}
            </div>
        </div>
    </div>
{% endblock %}
