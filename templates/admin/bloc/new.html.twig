{% extends 'baseAdmin.html.twig' %}

{% block title %}Ajouter une grande catégorie{% endblock %}

{% block body %}
    {% include 'admin/menu.html.twig' %}

    <div class="container">
        <div class="row mb-2">
            <div class="col text-left">
                <a href="{{ path('bloc_index_admin') }}" class="btn btn-secondary"><i class="fal fa-angle-left"></i> Retour</a>
            </div>
        </div>

        <div class="row">
            <div class="col">
                {{ include('admin/bloc/_form.html.twig') }}
            </div>
        </div>
    </div>

{% endblock %}
