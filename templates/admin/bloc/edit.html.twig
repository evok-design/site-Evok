{% extends 'baseAdmin.html.twig' %}

{% block title %}Modifier le groupe{% endblock %}

{% block body %}

    {% include 'admin/menu.html.twig' %}

    <div class="container">
        <div class="row">
            <div class="col">
                <a href="{{ path('bloc_index_admin') }}" class="btn btn-secondary"><i class="fal fa-angle-left"></i> Retour à la liste</a>
            </div>
        </div>
        <div class="row mt-3">
            <div class="col">
                <h1>Modifier {{ bloc.label }}</h1>
            </div>
        </div>
        <div class="row">
            <div class="col">
                {{ include('admin/bloc/_form.html.twig', {'button_label': 'Modifier'}) }}
            </div>
        </div>
    </div>

{% endblock %}
