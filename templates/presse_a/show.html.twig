{% extends 'base.html.twig' %}

{% block title %}PresseA{% endblock %}

{% block body %}
    <h1>PresseA</h1>

    <table class="table">
        <tbody>
            <tr>
                <th>Id</th>
                <td>{{ presse_a.id }}</td>
            </tr>
            <tr>
                <th>Titre</th>
                <td>{{ presse_a.titre }}</td>
            </tr>
            <tr>
                <th>Image</th>
                <td>{{ presse_a.image }}</td>
            </tr>
            <tr>
                <th>Lien_site</th>
                <td>{{ presse_a.lienSite }}</td>
            </tr>
            <tr>
                <th>CreatedAt</th>
                <td>{{ presse_a.createdAt ? presse_a.createdAt|date('Y-m-d H:i:s') : '' }}</td>
            </tr>
        </tbody>
    </table>

    <a href="{{ path('presse_a_index') }}">back to list</a>

    <a href="{{ path('presse_a_edit', {'id': presse_a.id}) }}">edit</a>

    {{ include('presse_a/_delete_form.html.twig') }}
{% endblock %}
